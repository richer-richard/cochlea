//! The `cochlea` binary: `render`, `probe`, `lint`, `spectro`.

fn main() {
    // Subcommands land phase by phase; `probe` ships in P3, the full set in
    // P4 (docs/plan.md).
    eprintln!("cochlea: no subcommands implemented yet (see docs/plan.md)");
    std::process::exit(2);
}
